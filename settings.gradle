pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "folium-port"

include("platform-api")
include("runtime-wasm")
include("render-webgpu")
